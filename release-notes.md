# MeshMux 0.4.0

配置页全面重做，并修复更新后系统代理丢失的问题。

## 界面重做

- 快速设置改为「状态 + 按钮」布局：主界面只显示订阅、Tailnet、WireGuard 的当前状态和必要按钮，表单收进弹窗，不再有铺在主界面的文本框。
- Tailscale 按钮按注册状态切换：已注册显示「配置」，未注册显示「登记 Tailscale」。
- WireGuard 面板改为多配置列表，导入多个 .conf 一目了然。
- 移除「生成并上传手机配置」入口（MeshMux 已放弃 Android，属历史遗留；CLI 侧能力保留）。
- 移除全部解释性注释文案。

## 更新后系统代理不再丢失

0.3.2 起，更新或重启后系统代理必然回到关闭状态——当时为解耦「核心启动」与「代理开关」，把托盘里所有自动开代理的逻辑删掉了，此后没有任何代码路径会恢复它。

- 新增代理意图持久化：托盘菜单与配置页每次显式切换系统代理，都会记录用户的选择（自动清理路径不写，避免误清除）。
- 托盘启动（含服务模式）后按记录的意图恢复系统代理，核心端口就绪前最多等待 15 秒，失败只记日志、不阻塞核心。
- 恢复动作全部在托盘（用户上下文）完成——服务进程跑在 LocalSystem 下，写不了登录用户的代理设置，无需提权。
- 首次安装（无记录）保持代理关闭，与原行为一致。

## 其他修复

- service：睡眠/唤醒后自动重启 tailscale 守护进程（PR #2）。此前唤醒后 tailscaled 不会恢复，Tailnet 处于离线状态，直到手动重启服务。

验证：`go build ./...`、`go vet` 与 `go test ./...` 通过；代理意图新增 4 个用例（缺省=关、写入读回、损坏文件按关处理、数据目录路径）。
