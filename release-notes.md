# MeshMux 0.3.9

0.3.8 的跟进修复，只改服务端的重试调度，不动其他行为。

0.3.8 已经让核心与 tailscale 各自独立退避，但实测发现退避其实没生效：启动一个组件只是把 goroutine 拉起来就返回成功，而「启动成功」会立刻把该组件的重试计数清零，所以一个拉起后马上退出的守护进程仍然每 5 秒被重试一次。日志里 tailscale 只是不再连带重启 mihomo 核心，5 秒刷屏本身还在。

- service：组件必须实际存活满 30 秒才算健康。只有到那时才清空重试计数，短命进程会继续把退避翻倍累积（5 秒起，上限 5 分钟）。
- service：启动路径、退避到期路径与恢复供电路径都不再单独清零计数，统一由进程退出时的存活时长决定。
- service：核心与 tailscale 共用同一套判断，两者的退避行为保持一致。

验证：`go build ./...`、`go vet` 与 `go test ./...` 通过；新增用例覆盖「连续三次短命运行后退避翻倍」与「存活满 30 秒后计数归零」。

已知边界：本次未改变组件缺失时的行为——那种情况下 `bin\tailscaled.exe` 仍然找不到，退避会照常累积，但根因要靠 0.3.8 的组件同步修复。若机器上存在被 wintun 卡死的残留核心进程，仍需重启才能清掉。
