# MeshMux 0.3.2

本次修复版重构 Windows 服务注册、核心更新和启动边界，重点解决安装失败会阻断应用、服务核心更新失效以及上游网络不可达导致本地程序无法启动的问题。

- Windows 安装器只安装程序、注册服务并启动托盘；不执行订阅检查、代理探测、控制器等待或十分钟级健康编排。
- 服务注册会从当前用户配置准备受保护的本地快照和服务核心；没有订阅、Tailnet 或上游代理时仍可完成安装并打开配置页面。
- Windows 服务使用唯一的 `ProgramData\\MeshMux\\bin\\mihomo.exe` 运行副本；安装器升级不会覆盖已有核心，显式核心更新失败会恢复旧核心。
- 系统代理由用户单独控制。核心启动、托盘启动、配置页面和上游代理可用性不再互相绑定。
- 核心下载要求组件 SHA-256 或 GitHub asset digest；未指定版本时按匹配的 MeshMux 核心发布查找，明确版本仍可锁定。
- 继续保留原子替换、跨进程互斥、订阅解析、归档清理和敏感日志脱敏修复。

提供 Windows x64 安装包、Linux amd64 应用包及 SHA-256 校验文件。核心、面板和 GeoIP 均通过发布流程中的内容哈希校验；需要 Tailnet 入站转发的构建必须使用包含该功能的 MeshMux 核心资产。

已有服务升级保留独立数据目录与 Tailnet 身份，不需要主动退出登录。安装器不把历史版本目录作为回滚目标，也不会自动恢复旧应用包。

验证边界：源码单元测试、vet、Windows CLI/托盘交叉构建已通过；Windows 安装器和 Linux race 由本次 GitHub Actions 产包流程执行。真实全新安装、跨机器升级、休眠恢复和 Linux 图形桌面交互仍需使用生成的确切制品验收。
