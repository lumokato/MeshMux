# MeshMux 0.3.4

本次发布重构核心启动与服务监督，修复启动等待阻塞服务控制和后台网络任务跨越核心生命周期的问题。

- Windows 服务立即接受控制请求；核心准备、TUN、Tailnet 和代理状态不作为服务启动就绪门槛。
- 核心失败记录诊断并安排重试，不阻止托盘和配置页面使用。
- 核心停止失败不立即启动替代实例；进程退出超时明确报错。
- 网络后处理随所属核心取消并结束，避免旧任务在重启后继续修改网络。
- 启动不再重新扫描脱敏整套历史日志；保留写入脱敏与大小轮转。
- 新配置默认启用 TUN 与自动路由，保留用户显式设置。
- 安装不以订阅或代理连通性为门槛；不自动恢复旧应用包或旧核心。

提供 Windows x64 安装器、Linux amd64 应用包及 SHA-256 文件。内置核心为 v1.19.29-meshmux.2，保留 MeshMux Tailnet 入站功能。本次不修改用户 Tailnet 数据，也不要求退出登录。

验证：Windows 本地 173 项测试、go vet 通过；发布流程分别构建并测试 Windows/Linux，并校验产物哈希。未宣称完成本版本的全新安装、跨机器升级、冷启动、睡眠恢复或 Linux 桌面验收。
